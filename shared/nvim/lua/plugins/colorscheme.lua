-- ==================================================================
-- カラースキーム
--   プラグインの「導入」だけここで行い、実際にどの配色を当てるかは
--   profile.lua（仕事/プライベート）側で切り替える例にしている。
--   → 環境差分が一目で分かるサンプルを兼ねている。
-- ==================================================================
return {
  {
    "folke/tokyonight.nvim",
    lazy = false,    -- 起動時すぐ読み込む
    priority = 1000, -- 他プラグインより先に読み込む（配色は最優先）
  },
}
