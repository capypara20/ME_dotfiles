# 各マシン固有のローカル上書き（git管理しない）
#   ~/.config/nvim は shared/nvim へのリンクなので、local.lua の実体は
#   リポジトリ内(shared/nvim/lua/local.lua)に置かれる。これを無視する。
shared/nvim/lua/local.lua

# 「*.local」という名前のローカルファイルは一律 git 管理外にする運用
*.local

# install スクリプトが作るバックアップ
*.bak

# OS が勝手に作るゴミ
.DS_Store
Thumbs.db
