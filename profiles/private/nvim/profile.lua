-- ==================================================================
-- プライベート環境の上書き（neovim）
--   この内容が ~/.config/nvim/lua/profile.lua としてリンクされ、
--   init.lua の末尾 require("profile") で最後に適用される。
--   「共通の土台」を壊さずに、ここだけで環境差分を表現する。
-- ==================================================================
vim.g.dotfiles_profile = "private"

-- 例) プライベートでは明るめの配色
pcall(vim.cmd.colorscheme, "tokyonight-storm")

-- ここに「プライベートPCだけで効かせたい設定」を書く
--   例: 個人用のキーマップ、趣味プロジェクト向けの LSP 設定 など
