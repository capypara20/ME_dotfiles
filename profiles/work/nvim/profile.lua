-- ==================================================================
-- 仕事環境の上書き（neovim）
--   この内容が ~/.config/nvim/lua/profile.lua としてリンクされ、
--   init.lua の末尾 require("profile") で最後に適用される。
--   「共通の土台」を壊さずに、ここだけで環境差分を表現する。
-- ==================================================================
vim.g.dotfiles_profile = "work"

-- 例) 仕事では落ち着いた暗めの配色
pcall(vim.cmd.colorscheme, "tokyonight-night")

-- ここに「仕事PCだけで効かせたい設定」を書く
--   例: 社内プロキシ前提の設定、業務で使う言語の LSP、
--       会社のコーディング規約に合わせたフォーマッタ設定 など
-- 注意: APIキーや社内ホスト名などの秘密情報はここに直書きせず、
--       git管理外のファイル(*.local 等)に分離すること。
