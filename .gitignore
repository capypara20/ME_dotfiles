# install スクリプトが作る退避ファイル
*.bak
*.bak-*

# このPC専用の設定（他のPCへ持っていかない）
*.local.ps1
*.local.sh
*.local.lua
local.gitconfig

# 認証情報（絶対にコミットしない。念のための保険）
.credentials.json
*credentials*
*.pem
*.key
id_rsa*
.env

# OS が勝手に作るファイル
Thumbs.db
desktop.ini
.DS_Store
